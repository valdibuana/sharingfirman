<!DOCTYPE html>
<html lang="id">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Persahabatan Yang Menyelamatkan Masa Depan</title>
  <link
    href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,700;0,900;1,700&family=DM+Sans:wght@300;400;500;600&family=Cinzel:wght@700;900&display=swap"
    rel="stylesheet">
  <style>
    *,
    *::before,
    *::after {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      --gold: #D4A843;
      --gold-light: #F0C96A;
      --cream: #FAF5E9;
      --deep: #0D0D0D;
      --navy: #0F1B2D;
      --warm: #2C1810;
      --text: #1A1A1A;
      --accent: #8B2FC9;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: 'DM Sans', sans-serif;
      background: var(--deep);
      color: var(--cream);
      overflow-x: hidden;
    }

    /* ── SLIDE SYSTEM ── */
    .slide {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: 60px 40px;
      position: relative;
      overflow: hidden;
    }

    /* ── SLIDE 1: COVER ── */
    .slide-cover {
      background: var(--navy);
      text-align: center;
    }

    .slide-cover::before {
      content: '';
      position: absolute;
      inset: 0;
      background:
        radial-gradient(ellipse 80% 60% at 50% 0%, rgba(212, 168, 67, 0.18) 0%, transparent 70%),
        radial-gradient(ellipse 60% 80% at 80% 100%, rgba(139, 47, 201, 0.12) 0%, transparent 70%);
    }

    .stars {
      position: absolute;
      inset: 0;
      overflow: hidden;
      pointer-events: none;
    }

    .star {
      position: absolute;
      border-radius: 50%;
      background: white;
      animation: twinkle var(--d, 3s) ease-in-out infinite alternate;
    }

    @keyframes twinkle {
      from {
        opacity: 0.1;
        transform: scale(0.8);
      }

      to {
        opacity: 0.9;
        transform: scale(1.2);
      }
    }

    .cover-tag {
      font-family: 'DM Sans', sans-serif;
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 5px;
      text-transform: uppercase;
      color: var(--gold);
      margin-bottom: 28px;
      opacity: 0;
      animation: fadeUp 0.8s 0.3s forwards;
    }

    .cover-title {
      font-family: 'Playfair Display', serif;
      font-size: clamp(36px, 7vw, 80px);
      font-weight: 900;
      line-height: 1.05;
      color: var(--cream);
      margin-bottom: 16px;
      opacity: 0;
      animation: fadeUp 0.9s 0.5s forwards;
    }

    .cover-title span {
      color: var(--gold);
      font-style: italic;
    }

    .cover-sub {
      font-size: 16px;
      font-weight: 300;
      color: rgba(250, 245, 233, 0.6);
      letter-spacing: 2px;
      margin-bottom: 48px;
      opacity: 0;
      animation: fadeUp 0.9s 0.7s forwards;
    }

    .cover-verse-box {
      border: 1px solid rgba(212, 168, 67, 0.4);
      border-radius: 16px;
      padding: 28px 36px;
      max-width: 580px;
      background: rgba(212, 168, 67, 0.06);
      backdrop-filter: blur(12px);
      opacity: 0;
      animation: fadeUp 1s 0.9s forwards;
    }

    .verse-ref {
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 3px;
      color: var(--gold);
      text-transform: uppercase;
      margin-bottom: 12px;
    }

    .verse-text {
      font-family: 'Playfair Display', serif;
      font-size: 18px;
      font-style: italic;
      line-height: 1.7;
      color: var(--cream);
    }

    .scroll-hint {
      position: absolute;
      bottom: 32px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 8px;
      opacity: 0;
      animation: fadeUp 1s 1.4s forwards;
    }

    .scroll-hint span {
      font-size: 10px;
      letter-spacing: 3px;
      color: rgba(250, 245, 233, 0.4);
      text-transform: uppercase;
    }

    .scroll-arrow {
      width: 20px;
      height: 20px;
      border-right: 2px solid rgba(212, 168, 67, 0.5);
      border-bottom: 2px solid rgba(212, 168, 67, 0.5);
      transform: rotate(45deg);
      animation: bounce 2s ease-in-out infinite;
    }

    @keyframes bounce {

      0%,
      100% {
        transform: rotate(45deg) translateY(0);
      }

      50% {
        transform: rotate(45deg) translateY(6px);
      }
    }

    /* ── OPENING SLIDE ── */
    .slide-opening {
      background: linear-gradient(160deg, #0F1B2D 0%, #1A0A2E 100%);
      text-align: center;
    }

    .slide-opening::before {
      content: '';
      position: absolute;
      top: -100px;
      left: 50%;
      transform: translateX(-50%);
      width: 600px;
      height: 600px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(212, 168, 67, 0.12) 0%, transparent 70%);
      pointer-events: none;
    }

    /* ── SECTION HEADER ── */
    .section-number {
      font-family: 'Cinzel', serif;
      font-size: 120px;
      font-weight: 900;
      color: rgba(212, 168, 67, 0.08);
      position: absolute;
      top: 20px;
      left: 50%;
      transform: translateX(-50%);
      user-select: none;
      pointer-events: none;
      letter-spacing: -4px;
    }

    /* ── SLIDE BAGIAN 1 ── */
    .slide-b1 {
      background: linear-gradient(135deg, #0D0D0D 0%, #1C0A00 100%);
    }

    .slide-b1::after {
      content: '';
      position: absolute;
      bottom: 0;
      right: 0;
      width: 400px;
      height: 400px;
      background: radial-gradient(circle at bottom right, rgba(212, 168, 67, 0.1), transparent 70%);
      pointer-events: none;
    }

    /* ── SLIDE BAGIAN 2 ── */
    .slide-b2 {
      background: linear-gradient(135deg, #050D1A 0%, #0A1428 100%);
    }

    .slide-b2::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 500px;
      height: 500px;
      background: radial-gradient(circle at top left, rgba(139, 47, 201, 0.08), transparent 70%);
      pointer-events: none;
    }

    /* ── SLIDE REFLEKSI ── */
    .slide-refleksi {
      background: linear-gradient(160deg, #0A0A0A 0%, #1A1A0A 100%);
    }

    /* ── SLIDE PENUTUP ── */
    .slide-closing {
      background: linear-gradient(160deg, var(--navy) 0%, #1A0A2E 100%);
      text-align: center;
    }

    /* ── SHARED CONTENT STYLES ── */
    .content-wrap {
      max-width: 800px;
      width: 100%;
      position: relative;
      z-index: 2;
    }

    .section-label {
      display: inline-block;
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 4px;
      text-transform: uppercase;
      color: var(--gold);
      border: 1px solid rgba(212, 168, 67, 0.3);
      padding: 6px 16px;
      border-radius: 100px;
      margin-bottom: 24px;
    }

    .slide-heading {
      font-family: 'Playfair Display', serif;
      font-size: clamp(28px, 5vw, 52px);
      font-weight: 900;
      line-height: 1.15;
      color: var(--cream);
      margin-bottom: 20px;
    }

    .slide-heading em {
      color: var(--gold);
      font-style: italic;
    }

    .slide-body {
      font-size: 16px;
      line-height: 1.85;
      color: rgba(250, 245, 233, 0.75);
      margin-bottom: 32px;
      font-weight: 300;
    }

    /* ── QUOTE BLOCK ── */
    .quote-block {
      border-left: 3px solid var(--gold);
      padding: 20px 28px;
      background: rgba(212, 168, 67, 0.06);
      border-radius: 0 12px 12px 0;
      margin: 28px 0;
      position: relative;
    }

    .quote-block::before {
      content: '"';
      font-family: 'Playfair Display', serif;
      font-size: 80px;
      color: rgba(212, 168, 67, 0.15);
      position: absolute;
      top: -10px;
      left: 12px;
      line-height: 1;
    }

    .quote-block .q-text {
      font-family: 'Playfair Display', serif;
      font-size: 19px;
      font-style: italic;
      line-height: 1.7;
      color: var(--cream);
      padding-left: 16px;
    }

    .quote-block .q-ref {
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 2px;
      color: var(--gold);
      margin-top: 12px;
      padding-left: 16px;
      text-transform: uppercase;
    }

    /* ── TRUTH CARDS ── */
    .truth-cards {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 16px;
      margin: 28px 0;
    }

    .truth-card {
      background: rgba(250, 245, 233, 0.04);
      border: 1px solid rgba(212, 168, 67, 0.2);
      border-radius: 16px;
      padding: 24px 20px;
      text-align: center;
      transition: all 0.3s ease;
      cursor: default;
    }

    .truth-card:hover {
      background: rgba(212, 168, 67, 0.08);
      border-color: rgba(212, 168, 67, 0.5);
      transform: translateY(-4px);
    }

    .truth-card .icon {
      font-size: 28px;
      margin-bottom: 12px;
      display: block;
    }

    .truth-card .t-title {
      font-size: 13px;
      font-weight: 600;
      color: var(--gold);
      margin-bottom: 8px;
      letter-spacing: 0.5px;
    }

    .truth-card .t-desc {
      font-size: 13px;
      color: rgba(250, 245, 233, 0.6);
      line-height: 1.6;
      font-weight: 300;
    }

    /* ── DIVIDER ── */
    .gold-divider {
      width: 60px;
      height: 2px;
      background: linear-gradient(90deg, transparent, var(--gold), transparent);
      margin: 24px auto;
      border: none;
    }

    /* ── NAMES ROW ── */
    .names-row {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin: 20px 0;
      justify-content: center;
    }

    .name-pill {
      background: rgba(212, 168, 67, 0.1);
      border: 1px solid rgba(212, 168, 67, 0.3);
      color: var(--gold-light);
      font-size: 13px;
      font-weight: 600;
      letter-spacing: 1px;
      padding: 8px 20px;
      border-radius: 100px;
    }

    /* ── QUESTION BOX ── */
    .question-box {
      background: rgba(139, 47, 201, 0.08);
      border: 1px solid rgba(139, 47, 201, 0.3);
      border-radius: 16px;
      padding: 28px 32px;
      margin: 28px 0;
    }

    .question-box .q-header {
      font-size: 10px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      color: #B06EE8;
      margin-bottom: 16px;
    }

    .question-box .q-item {
      font-size: 16px;
      color: var(--cream);
      margin-bottom: 12px;
      padding-left: 20px;
      position: relative;
      line-height: 1.6;
    }

    .question-box .q-item::before {
      content: '→';
      position: absolute;
      left: 0;
      color: var(--gold);
    }

    /* ── COMPARISON ── */
    .compare-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
      margin: 28px 0;
    }

    .compare-col {
      border-radius: 16px;
      padding: 24px;
    }

    .compare-col.bad {
      background: rgba(180, 50, 50, 0.08);
      border: 1px solid rgba(180, 50, 50, 0.25);
    }

    .compare-col.good {
      background: rgba(50, 180, 100, 0.08);
      border: 1px solid rgba(50, 180, 100, 0.25);
    }

    .compare-col h4 {
      font-size: 11px;
      font-weight: 700;
      letter-spacing: 3px;
      text-transform: uppercase;
      margin-bottom: 16px;
    }

    .compare-col.bad h4 {
      color: #E87070;
    }

    .compare-col.good h4 {
      color: #70E8A0;
    }

    .compare-col li {
      list-style: none;
      font-size: 14px;
      line-height: 1.7;
      color: rgba(250, 245, 233, 0.75);
      margin-bottom: 8px;
      padding-left: 18px;
      position: relative;
    }

    .compare-col.bad li::before {
      content: '✕';
      position: absolute;
      left: 0;
      color: #E87070;
    }

    .compare-col.good li::before {
      content: '✓';
      position: absolute;
      left: 0;
      color: #70E8A0;
    }

    /* ── BIG STATEMENT ── */
    .big-statement {
      font-family: 'Playfair Display', serif;
      font-size: clamp(22px, 4vw, 40px);
      font-weight: 700;
      font-style: italic;
      line-height: 1.4;
      color: var(--cream);
      text-align: center;
      padding: 40px;
      position: relative;
    }

    .big-statement::before,
    .big-statement::after {
      content: '—';
      display: block;
      width: 40px;
      height: 2px;
      background: var(--gold);
      margin: 0 auto 20px;
    }

    .big-statement::after {
      margin: 20px auto 0;
    }

    .big-statement span {
      color: var(--gold);
    }

    /* ── CLOSING CTA ── */
    .closing-box {
      max-width: 600px;
      text-align: center;
      position: relative;
      z-index: 2;
    }

    .closing-icon {
      font-size: 48px;
      margin-bottom: 24px;
      display: block;
      animation: pulse 2s ease-in-out infinite;
    }

    @keyframes pulse {

      0%,
      100% {
        transform: scale(1);
      }

      50% {
        transform: scale(1.08);
      }
    }

    .closing-title {
      font-family: 'Playfair Display', serif;
      font-size: clamp(28px, 5vw, 52px);
      font-weight: 900;
      color: var(--cream);
      margin-bottom: 20px;
      line-height: 1.2;
    }

    .closing-title span {
      color: var(--gold);
      font-style: italic;
    }

    .closing-body {
      font-size: 16px;
      line-height: 1.85;
      color: rgba(250, 245, 233, 0.7);
      font-weight: 300;
      margin-bottom: 36px;
    }

    .closing-action-text {
      font-size: 14px;
      font-weight: 600;
      letter-spacing: 2px;
      color: var(--gold);
      text-transform: uppercase;
      padding: 16px 36px;
      border: 1.5px solid var(--gold);
      border-radius: 100px;
      display: inline-block;
      background: rgba(212, 168, 67, 0.06);
    }

    /* ── SEPARATOR ── */
    .slide-separator {
      display: flex;
      align-items: center;
      gap: 20px;
      margin: 48px 0 24px;
    }

    .slide-separator::before,
    .slide-separator::after {
      content: '';
      flex: 1;
      height: 1px;
      background: linear-gradient(90deg, transparent, rgba(212, 168, 67, 0.3), transparent);
    }

    .slide-separator span {
      color: var(--gold);
      font-size: 18px;
    }

    /* ── NAV BAR ── */
    .nav-bar {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 100;
      background: rgba(13, 13, 13, 0.85);
      backdrop-filter: blur(16px);
      border-bottom: 1px solid rgba(212, 168, 67, 0.15);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 14px 32px;
    }

    .nav-logo {
      font-family: 'Cinzel', serif;
      font-size: 13px;
      letter-spacing: 2px;
      color: var(--gold);
    }

    .nav-links {
      display: flex;
      gap: 24px;
      list-style: none;
    }

    .nav-links a {
      text-decoration: none;
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 2px;
      text-transform: uppercase;
      color: rgba(250, 245, 233, 0.5);
      transition: color 0.2s;
    }

    .nav-links a:hover {
      color: var(--gold);
    }

    /* ── ANIMATIONS ── */
    @keyframes fadeUp {
      from {
        opacity: 0;
        transform: translateY(24px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .reveal {
      opacity: 0;
      transform: translateY(30px);
      transition: opacity 0.8s ease, transform 0.8s ease;
    }

    .reveal.visible {
      opacity: 1;
      transform: translateY(0);
    }

    .reveal-delay-1 {
      transition-delay: 0.1s;
    }

    .reveal-delay-2 {
      transition-delay: 0.2s;
    }

    .reveal-delay-3 {
      transition-delay: 0.35s;
    }

    .reveal-delay-4 {
      transition-delay: 0.5s;
    }

    /* ── RESPONSIVE ── */
    @media (max-width: 600px) {
      .compare-grid {
        grid-template-columns: 1fr;
      }

      .nav-links {
        display: none;
      }

      .slide {
        padding: 80px 24px 48px;
      }
    }

    /* ── HIGHLIGHT ROW ── */
    .highlight-row {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin: 20px 0;
    }

    .hi-chip {
      background: rgba(212, 168, 67, 0.1);
      border: 1px solid rgba(212, 168, 67, 0.25);
      border-radius: 8px;
      padding: 12px 18px;
      font-size: 14px;
      color: rgba(250, 245, 233, 0.85);
      line-height: 1.5;
      flex: 1;
      min-width: 180px;
    }

    .hi-chip strong {
      display: block;
      color: var(--gold);
      font-size: 11px;
      letter-spacing: 2px;
      text-transform: uppercase;
      margin-bottom: 4px;
    }

    .page-num {
      position: absolute;
      bottom: 24px;
      right: 32px;
      font-size: 11px;
      font-weight: 600;
      letter-spacing: 3px;
      color: rgba(212, 168, 67, 0.3);
      z-index: 2;
    }
  </style>
</head>

<body>

  <!-- NAV -->
  <nav class="nav-bar">
    <div class="nav-logo">✦ Youth Sharing</div>
    <ul class="nav-links">
      <li><a href="#opening">Pembukaan</a></li>
      <li><a href="#bagian1">Daud & Yonatan</a></li>
      <li><a href="#bagian2">Daniel</a></li>
      <li><a href="#refleksi">Refleksi</a></li>
      <li><a href="#penutup">Penutup</a></li>
      <li><a href="games.html" style="color:var(--gold)">🎮 Games</a></li>
    </ul>
  </nav>

  <!-- ═══════════════════════════════ COVER ═══════════════════════════════ -->
  <section class="slide slide-cover" id="cover">
    <div class="stars" id="stars"></div>
    <div class="cover-tag">Youth Sharing · Firman Tuhan</div>
    <h1 class="cover-title">Persahabatan Yang<br><span>Menyelamatkan</span><br>Masa Depan</h1>
    <p class="cover-sub">Amsal · 1 Samuel · Daniel</p>
    <div class="cover-verse-box">
      <div class="verse-ref">📖 Amsal 13:20</div>
      <div class="verse-text">"Siapa bergaul dengan orang bijak menjadi bijak, tetapi siapa berteman dengan orang bebal
        menjadi malang."</div>
    </div>
    <div class="scroll-hint">
      <span>Scroll</span>
      <div class="scroll-arrow"></div>
    </div>
  </section>

  <!-- ═══════════════════════════════ OPENING ═══════════════════════════════ -->
  <section class="slide slide-opening" id="opening">
    <div class="content-wrap">
      <div class="section-label reveal">🙏 Shalom, Teman-Teman!</div>
      <h2 class="slide-heading reveal reveal-delay-1">Persahabatan bisa <em>menentukan arah hidupmu.</em></h2>
      <div class="slide-separator reveal reveal-delay-2"><span>✦</span></div>
      <p class="slide-body reveal reveal-delay-2">
        Hari ini kita tidak sekadar berbicara tentang teman. Kita berbicara tentang bagaimana orang-orang di sekitarmu
        bisa membawamu <strong style="color:var(--gold)">lebih dekat atau lebih jauh dari Tuhan</strong> — dan bagaimana
        persahabatan yang benar bisa benar-benar menyelamatkan masa depanmu.
      </p>
      <p class="slide-body reveal reveal-delay-3">Kita akan belajar dari dua kisah nyata di Alkitab:</p>
      <div class="names-row reveal reveal-delay-3">
        <div class="name-pill">👑 Daud &amp; Yonatan</div>
        <div class="name-pill">🔥 Daniel, Hananya, Misael &amp; Azarya</div>
      </div>
      <div class="big-statement reveal reveal-delay-4">
        Persahabatan yang benar bukan hanya soal kebersamaan,<br>
        tetapi soal <span>iman dan komitmen.</span>
      </div>
    </div>
    <div class="page-num">01</div>
  </section>

  <!-- ═══════════════════════════════ BAGIAN 1 ═══════════════════════════════ -->
  <section class="slide slide-b1" id="bagian1">
    <div class="section-number">I</div>
    <div class="content-wrap">
      <div class="section-label reveal">📖 Bagian 1 · 1 Samuel 18</div>
      <h2 class="slide-heading reveal reveal-delay-1">Daud &amp; Yonatan —<br><em>Sahabat yang Rela Berkorban</em></h2>
      <p class="slide-body reveal reveal-delay-2">
        Daud adalah orang yang diurapi menjadi raja berikutnya. Yonatan adalah putra Raja Saul — pewaris tahta yang sah.
        Secara logika dunia? <strong style="color:var(--cream)">Mereka seharusnya menjadi musuh.</strong>
      </p>

      <div class="quote-block reveal reveal-delay-2">
        <div class="q-text">"Yonatan mengikat perjanjian dengan Daud, karena ia mengasihi dia seperti dirinya sendiri."
        </div>
        <div class="q-ref">1 Samuel 18:1</div>
      </div>

      <div class="truth-cards reveal reveal-delay-3">
        <div class="truth-card">
          <span class="icon">🚫</span>
          <div class="t-title">Tidak Iri</div>
          <div class="t-desc">Yonatan tidak iri saat Daud diberkati Tuhan melebihi dirinya</div>
        </div>
        <div class="truth-card">
          <span class="icon">🛡️</span>
          <div class="t-title">Melindungi</div>
          <div class="t-desc">Ia bahkan melindungi Daud dari ayahnya sendiri, Raja Saul</div>
        </div>
        <div class="truth-card">
          <span class="icon">🤝</span>
          <div class="t-title">Rela Berkorban</div>
          <div class="t-desc">Demi kebaikan sahabat, ia rela melepas haknya sendiri</div>
        </div>
      </div>

      <div class="question-box reveal reveal-delay-4">
        <div class="q-header">🙋 Pertanyaan Untuk Kita</div>
        <div class="q-item">Apakah kita punya teman yang mendukung panggilan Tuhan dalam hidup kita?</div>
        <div class="q-item">Atau justru menarik kita menjauh dari-Nya?</div>
        <div class="q-item">Persahabatan yang sehat tidak takut melihat kita naik — karena pusatnya bukan ego, tapi
          kasih.</div>
      </div>
    </div>
    <div class="page-num">02</div>
  </section>

  <!-- ═══════════════════════════════ BAGIAN 2 ═══════════════════════════════ -->
  <section class="slide slide-b2" id="bagian2">
    <div class="section-number">II</div>
    <div class="content-wrap">
      <div class="section-label reveal">📖 Bagian 2 · Daniel 1 &amp; 3</div>
      <h2 class="slide-heading reveal reveal-delay-1">Daniel &amp; Sahabatnya —<br><em>Menjaga Iman Bersama</em></h2>

      <div class="names-row reveal reveal-delay-2">
        <div class="name-pill">🦁 Daniel</div>
        <div class="name-pill">🔥 Shadrach</div>
        <div class="name-pill">🔥 Meshach</div>
        <div class="name-pill">🔥 Abednego</div>
      </div>

      <p class="slide-body reveal reveal-delay-2">
        Mereka dibuang ke Babel — budaya asing, tekanan besar, godaan besar untuk <strong
          style="color:var(--cream)">kompromi dengan dunia.</strong> Tapi mereka memilih berbeda.
      </p>

      <div class="quote-block reveal reveal-delay-3">
        <div class="q-text">"Daniel berketetapan untuk tidak menajiskan dirinya."</div>
        <div class="q-ref">Daniel 1:8</div>
      </div>

      <div class="quote-block reveal reveal-delay-3">
        <div class="q-text">"Allah kami sanggup melepaskan kami… tetapi seandainya tidak…"</div>
        <div class="q-ref">Daniel 3:17–18</div>
      </div>

      <div class="highlight-row reveal reveal-delay-4">
        <div class="hi-chip"><strong>💪 Menguatkan</strong>Menguatkan saat iman kita mulai goyah dan lemah</div>
        <div class="hi-chip"><strong>⚠️ Mengingatkan</strong>Mengingatkan saat kita hampir kompromi dengan dunia</div>
        <div class="hi-chip"><strong>🔗 Berdiri Bersama</strong>Tidak saling meninggalkan saat ada tekanan dan ancaman
        </div>
      </div>

      <div class="question-box reveal reveal-delay-4">
        <div class="q-header">🙋 Pertanyaan Untuk Kita</div>
        <div class="q-item">Apakah lingkaran pertemananmu membuat kamu lebih dekat dengan Tuhan?</div>
        <div class="q-item">Atau makin menjauh? Karena teman menentukan standar hidupmu.</div>
      </div>
    </div>
    <div class="page-num">03</div>
  </section>

  <!-- ═══════════════════════════════ REFLEKSI ═══════════════════════════════ -->
  <section class="slide slide-refleksi" id="refleksi">
    <div class="section-number">III</div>
    <div class="content-wrap">
      <div class="section-label reveal">💬 Refleksi · Untuk Kita Hari Ini</div>
      <h2 class="slide-heading reveal reveal-delay-1">Dunia Mengukur Pertemanan<br><em>Dengan Cara Yang Berbeda</em>
      </h2>

      <div class="compare-grid reveal reveal-delay-2">
        <div class="compare-col bad">
          <h4>Dunia Bilang</h4>
          <ul>
            <li>Seberapa seru temanmu</li>
            <li>Seberapa populer dia</li>
            <li>Seberapa banyak followers-nya</li>
            <li>Kamu sukses = dia dukung</li>
            <li>Tertawa bersama</li>
          </ul>
        </div>
        <div class="compare-col good">
          <h4>Alkitab Berkata</h4>
          <ul>
            <li>Kesetiaan yang nyata</li>
            <li>Kejujuran yang membangun</li>
            <li>Komitmen rohani</li>
            <li>Kamu jatuh = dia masih ada</li>
            <li>Berdoa bersama</li>
          </ul>
        </div>
      </div>

      <div class="big-statement reveal reveal-delay-3">
        Teman yang benar bukan hanya mendukung <span>mimpimu</span>,<br>
        tetapi mendukung <span>panggilan Tuhan</span> dalam hidupmu.
      </div>

      <div class="highlight-row reveal reveal-delay-4">
        <div class="hi-chip"><strong>🤲 Ada Saat Jatuh</strong>Bukan hanya saat sukses dan saat segalanya baik-baik saja
        </div>
        <div class="hi-chip"><strong>🙏 Berdoa Bersama</strong>Bukan hanya tertawa — tapi juga bisa berlutut bersama
        </div>
        <div class="hi-chip"><strong>📣 Mendukung Panggilan</strong>Mendukung kehendak Tuhan, bukan hanya keinginanmu
        </div>
      </div>
    </div>
    <div class="page-num">04</div>
  </section>

  <!-- ═══════════════════════════════ PENUTUP ═══════════════════════════════ -->
  <section class="slide slide-closing" id="penutup">
    <div class="stars" id="stars2"></div>
    <div class="closing-box">
      <span class="closing-icon">✝️</span>
      <h2 class="closing-title reveal">Jadi, <span>Siapa</span> Sahabatmu?</h2>
      <p class="closing-body reveal reveal-delay-1">
        Persahabatan bukan sekadar kebersamaan. Persahabatan adalah tentang ke mana kamu berjalan bersama. Apakah menuju
        Tuhan — atau menjauh dari-Nya?
      </p>
      <div class="cover-verse-box reveal reveal-delay-2" style="margin: 0 auto 32px;">
        <div class="verse-ref">📖 Amsal 13:20</div>
        <div class="verse-text">"Siapa bergaul dengan orang bijak menjadi bijak, tetapi siapa berteman dengan orang
          bebal menjadi malang."</div>
      </div>
      <p class="closing-body reveal reveal-delay-3">
        Jadilah teman seperti <strong style="color:var(--gold)">Yonatan</strong> — yang tidak iri, yang melindungi, yang
        setia.<br>
        Jadilah teman seperti <strong style="color:var(--gold)">Daniel</strong> — yang berdiri teguh dalam iman,
        bersama-sama.
      </p>
      <div class="closing-action-text reveal reveal-delay-4">
        ✦ God bless your friendships ✦
      </div>
      <a href="games.html"
        style="display:inline-block;margin-top:20px;padding:14px 36px;background:linear-gradient(135deg,var(--gold),#F0C96A);color:#0D0D0D;font-size:14px;font-weight:700;letter-spacing:1px;border-radius:100px;text-decoration:none;text-transform:uppercase;transition:all 0.3s;"
        class="reveal reveal-delay-4"
        onmouseover="this.style.transform='scale(1.05)';this.style.boxShadow='0 8px 32px rgba(212,168,67,0.3)'"
        onmouseout="this.style.transform='scale(1)';this.style.boxShadow='none'">🎮 Main Games Interaktif</a>
    </div>
    <div class="page-num">05</div>
  </section>

  <script>
    // Stars
    function makeStars(containerId, count) {
      const c = document.getElementById(containerId);
      if (!c) return;
      for (let i = 0; i < count; i++) {
        const s = document.createElement('div');
        s.className = 'star';
        const size = Math.random() * 2.5 + 0.5;
        s.style.cssText = `
        width:${size}px; height:${size}px;
        left:${Math.random() * 100}%; top:${Math.random() * 100}%;
        --d:${(Math.random() * 3 + 2).toFixed(1)}s;
        animation-delay:${(Math.random() * 4).toFixed(1)}s;
      `;
        c.appendChild(s);
      }
    }
    makeStars('stars', 80);
    makeStars('stars2', 60);

    // Scroll reveal
    const reveals = document.querySelectorAll('.reveal');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(e => {
        if (e.isIntersecting) {
          e.target.classList.add('visible');
          observer.unobserve(e.target);
        }
      });
    }, { threshold: 0.15 });
    reveals.forEach(r => observer.observe(r));
  </script>
</body>

</html>